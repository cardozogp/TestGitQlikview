///$tab QlikView Development Framework
// First Base Variable Include ,Use relative path to 1.init.qvs
// Contains base searchpath in this container


let vG.BasePath=; 
// Basic search for InitLink.qvs in container base 
// InitLink.qvs will find and connect to 1.Init.qvs
$(Include=..\..\..\..\InitLink.qvs);
$(Include=..\..\..\InitLink.qvs);
$(Include=..\..\InitLink.qvs);
$(Include=..\InitLink.qvs);
Exit script when '$(vG.BasePath)'= '';


// Use function: call LCGV('ContainerName','[optional, Specific folder],['optional, pick container map from shared folder']) 
// Use for connection to link with a valid container by use of Container Map


// call LCGV('Oracle');
// call LCGV('ASales','ConnString');

// Create a folder  using 6.CreateFolder.qvs
// call CreateFolder('$(vG.QVDPath)\03.Example_Northwind'); // will create folder $(vG.QVDPath)\0.Example_Northwind

// Locale for English
$(Include=$(vG.LocalePath)\1.US.qvs);

// $(Include=$(vG.BaseVariablePath)\0.Example_4.acme_Container.qvs);

// Example of reusing sub functions in Deployment Framework, in this case DoDir

// call DoDir ('$(vG.RootPath)');
